version: "3.9"
services:
  frontend:
    image: docker.io/${DOCKERHUB_USERNAME}/sistema-propiedad_intelectual:${APP_TAG:-latest}
    container_name: sistema-frontend
    restart: unless-stopped
    ports:
      - "${front_port}:80"
    env_file:
      - ../.env
    pull_policy: always
